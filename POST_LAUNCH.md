# 출시 후 할 일

출시 전에 손대면 위험하거나, 급하지 않아서 미뤄둔 것들입니다.
발견 시점과 근거를 같이 적어둡니다 — 나중에 "이걸 왜 미뤘더라"를 다시 추론하지 않기 위해서.

기준일: 2026-08-25 (첫 제출 9/1, 출시 완료 9/30)

---

## 0. 하단 탭 바 — 서랍을 걷어냅니다

**출시 후 첫 작업입니다** (2026-08-30, 윤아님 지정).

### 왜

지금은 햄버거 → 서랍에 일곱 칸입니다: Home, Inner Journal, Calendar,
Reflection, Monthly Review, Pricing, My Archetype.

핵심 루프는 홈에서 탭 한 번인데, **"쌓이는 느낌"을 만드는 캘린더와 저널이 서랍
뒤에 숨어 있습니다.** 재구독의 이유가 되는 화면이 두 단계 뒤에 있는 셈입니다.
Opal 같은 앱은 그 목적지들을 하단 탭에 상시 노출합니다 — 화면에 있는 것과
서랍 안에 있는 것은 사용률이 다릅니다.

햄버거는 데스크톱 웹의 관습이고, 2026년 iOS 앱에서는 그 자체로 오래돼 보입니다.

### 무엇으로

```
┌─────────────────────────────────────┐
│  🔥 3                          👤   │  ← 스트릭 + 아바타
│                                     │
│              ◯                      │  ← 홈: 원 하나
│                                     │
├─────────────────────────────────────┤
│   ◯ Today    ▤ Journal   ✦ Insights │  ← 하단 탭
└─────────────────────────────────────┘
```

| 탭 | 흡수 | 근거 |
| --- | --- | --- |
| **Today** | Home | 오늘 아침의 원. 앱의 얼굴 |
| **Journal** | Inner Journal + Calendar | 둘 다 "내가 쓴 것". 캘린더는 저널의 다른 뷰일 뿐 |
| **Insights** | Reflection + Monthly Review + My Archetype | 전부 "의미" 층 |
| 우상단 아바타 | Pricing + 설정 + 언어 + 로그아웃 | |

**Insights 탭이 이 개편의 진짜 이유입니다.** Pro가 주는 것(월간 리뷰, 패턴
분석)이 지금 서랍 두 칸에 흩어져 있어서 **뭘 사는 건지 보이지 않습니다.** 한
탭에 모으면 무료 사용자가 매일 그 탭을 지나면서 "여기 뭐가 더 있구나"를 알게
됩니다. 페이월 문구보다 그게 더 잘 팝니다.

### 왜 출시 전에 안 했나

두 가지입니다.

**내비게이션 변경은 그 시점에 할 수 있는 가장 위험한 변경이었습니다.** 모든
화면에 닿고, 미묘하게 깨진 것은 심사 중에 발견됩니다. 실기기에서 충분히
눌러볼 시간이 없었습니다.

**스크린샷이 전부 무효가 됩니다.** 내비게이션이 바뀌면 다섯 장을 다시 찍어야
하고, App Store 스크린샷은 새 버전을 제출해야만 교체됩니다. 이 결정은
스크린샷 촬영 앞이거나 출시 뒤여야 했고, 중간은 없었습니다.

**1.1로 미룬 게 오히려 유리합니다.** 해커톤 심사 기간이 10/1~10/13이라, 그
사이에 내비게이션 개편이 나가면 심사위원에게 살아 움직이는 앱으로 보입니다.

---

## 0-1. 가격 재검토 — 출시 후 4주

**언제** 유료 전환이 최소 스무 건쯤 쌓인 뒤. 그 전에는 데이터가 아니라 추측입니다.

### 왜 적어두나

2026-08-30에 Opal(스크린타임 차단 앱)이 연 ₩149,000(약 $106)을 받는 걸 보고
"$49.99가 너무 싼가"라는 질문이 나왔습니다. 출시 전에는 올리지 않기로 했습니다.

### 왜 지금 올리지 않았나

**Opal은 같은 카테고리가 아닙니다.** 그쪽은 고통을 없애줍니다 — 사람들이 가장 잘
지불하는 형태이고, 얼마나 아팠는지 이미 알고 있어서 `TIME SAVED` 같은 숫자를 화면에
박아둘 수 있습니다. 노바키츠는 의미를 더해줍니다. 좋은 것이지만 처음 보는 사람이
값을 매길 근거가 없고, 리뷰도 아직 없습니다.

**유료 사용자가 0명입니다.** 전환율을 모르는 상태에서 가격을 바꾸는 건 추측을
다른 추측으로 바꾸는 것입니다.

**7일 무료 체험이 계산에 들어갑니다.** 체험 → $49.99는 넘을 만한 문턱인데,
체험 → $106은 브랜드 없이는 다른 이야기입니다.

**제출 이틀 전에 상품을 다시 건드리는 위험도 있었습니다.** 두 구독이 겨우 정리된
상태였습니다.

### 왜 미루는 것이 안전한가

**신규 구독자 대상 인상은 App Store Connect에서 자유롭습니다.** 기존 구독자는 원래
가격에 남습니다(그들에게 올리려면 동의를 받아야 하고, 그건 별개의 마찰입니다).
지금이 아니면 안 되는 결정이 아닙니다.

반대 방향은 어렵습니다. 내리면 먼저 산 사람이 손해 본 것이 됩니다. 그래서 낮게
시작해서 올리는 쪽이 되돌릴 수 있는 방향입니다.

### 볼 것

- 체험 시작률 (페이월을 본 사람 중 몇 %가 체험을 시작하나)
- 체험 → 결제 전환율
- 월간 대 연간 비율. 지금 구조는 연간이 월간 8.3개월치라 연간을 강하게 밀고 있고,
  그 설계는 잘 되어 있습니다

### 먼저 볼 숫자는 연간이 아니라 월간일 수 있습니다

**$5.99/월이 오히려 낮은 편입니다.** 매일 AI 해석을 주는데 Calm이 월 $14.99입니다.
다만 월간을 올리면 연간과의 비율이 바뀌어 연간 유인이 약해집니다. 두 숫자를 따로
보지 말고 비율로 보세요.

### 인상의 전제 조건 — 증거 화면

위에서 "Opal은 고통을 없애주고 우리는 의미를 더해준다"고 적었는데, **그건 틀린
구분입니다.** pricing 페이지가 이미 고통을 말하고 있습니다:

- *"What you feel but can't say, your dreams already know. Feeling anxious for
  no reason?"* — 원인 모를 불안. 화면 시간보다 아픈 고통입니다
- *"The first 5 minutes determine your day. Do you check Instagram first
  thing?"* — 이건 아예 Opal의 영토입니다. **같은 고통을 반대편에서** 공략하는
  것이고요. Opal은 폰을 막고, 우리는 첫 5분에 할 더 나은 것을 줍니다

진짜 차이는 고통의 유무가 아니라 **나아졌다는 증거의 유무**입니다.

| | Opal | Novakitz |
| --- | --- | --- |
| 고통 | "4시간을 폰에 버렸다" | "이유 없이 불안하게 깼다" |
| 측정되나 | 시간, 숫자 | 직접적으론 아님 |
| **나아졌다는 증거** | **`TIME SAVED`** | **없음** |

Opal 홈에 `TIME SAVED`가 박혀 있는 이유가 그겁니다. 계속 돈을 내게 만드는 건
고통이 아니라 고통이 줄었다는 증거입니다.

**그래서 $106을 받으려면 용감해지는 게 아니라 증거 화면을 만들어야 합니다.**
재료는 이미 있습니다 — 월간 리뷰와 패턴 분석이 우리의 `TIME SAVED`입니다. 다만
두 가지가 막고 있습니다:

1. **Pro 전용입니다.** 살 이유를 증거로 만들려는데 증거가 유료 뒤에 있습니다
2. **서랍 안에 있습니다.** §0(탭 바)이 고치려는 게 이 부분입니다

무료 사용자에게 **숫자 하나**만 보여줘도 달라집니다 — "이번 달 아침 18번",
"지난달보다 불안한 아침이 3일 줄었습니다" 같은 것. Opal의 `TIME SAVED`와 같은
자리에 놓이는 물건입니다.

**4주 뒤에 전환율만 보고 올릴지 말지 정하지 마세요.** "무엇을 만들어야 올릴 수
있나"가 먼저입니다. 순서는 §0(탭 바) → 증거 화면 → 가격입니다.

---

## 0-2. 대화형 온보딩 — 타이핑되는 첫 화면

**언제** §0(탭 바) 다음. 1.1 또는 1.2.

### 지금 무엇이 되어 있나

출시 전에 **다섯 단계를 두 단계로 줄였습니다** (`5987535`, `3cbefa3`).
닉네임 하나, 그리고 선택 입력인 나이. 없앤 것은 생년월일 세 칸, 직업,
관심사, 꿈 목표, 그리고 닉네임 중복 검사입니다 — 중복 검사는 사실
표시 이름에 걸려 있던 것이라, 가입에서 쓴 이름을 프로필 설정에서
저장할 수 없는 상태였습니다(검증 함수 두 개가 갈라져 있었습니다).

**이탈의 원인 대부분은 이 다섯 단계였습니다.** 화면 수를 줄인 것이
효과의 큰 쪽이고, 아래는 그 위에 얹는 마감입니다.

### 무엇이 남았나

윤아님이 Renight 스크린샷으로 보여주신 형태입니다. 질문이 한 글자씩
타이핑되고, 답하면 다음 질문이 이어지는 — 폼이 아니라 대화로 보이는
화면. 애니메이션이 없더라도 **타이핑 효과만은** 있어야 그 느낌이 납니다.

```
        What should I call you?          ← 한 글자씩
        ┌───────────────────┐
        │                   │
        └───────────────────┘
```

`src/components/UserProfileForm.tsx`. 지금은 진행 점 두 개가 달린
단계형 폼입니다.

### 왜 출시 전에 안 했나

**신규 사용자가 보는 첫 화면이고, 여기서 깨지면 빠져나갈 길이
없습니다.** 다 타이핑되지 않는 줄, 키보드에 가려지는 입력칸, 넘길 수
없는 단계 — 어느 것이든 앱을 처음 연 사람을 문 앞에 세워둡니다. 다른
화면의 버그는 뒤로 가면 되지만 여기는 그게 안 됩니다. 실기기에서
충분히 눌러볼 시간이 없는 시점에 손대기에 가장 비싼 자리였습니다.

**측정할 수도 없습니다.** 이 작업의 목표는 가입 이탈률을 낮추는
것인데, 출시 전에는 비교할 신규 가입이 없습니다. 출시 후 2~3주치
이탈 지점을 보고 나면 타이핑이 문제였는지, 질문 자체가 문제였는지가
갈립니다.

**§0과 같은 코드를 건드리지 않습니다.** 순서를 뒤집어도 되지만,
탭 바가 리텐션에 더 크게 걸려 있어서 뒤에 둡니다.

---

## 0-3. Opal Timer 탭에서 가져올 것과 안 가져올 것

윤아님이 2026-08-31에 Opal의 Timer 탭 스크린샷을 보내셨습니다. 한 화면에
세 가지가 섞여 있어서, 셋을 따로 판단합니다.

### 가져옵니다 — 카드의 `⚡PRO` 뱃지 (§0과 같이)

§0-1에 "무료 사용자에게 뭘 사는 건지 안 보인다"고 적어둔 것을, 저 뱃지가
그대로 해결하고 있습니다. **잠긴 물건을 매일 눈으로 보게 하는 것**과
결제 화면에 들어가야 읽히는 문구는 다릅니다.

비용이 거의 없습니다 — 카드 컴포넌트에 뱃지 하나. §0의 Insights 탭에
월간 리뷰·패턴 분석 카드를 놓을 때 같이 붙입니다. **§0과 분리해서 하지
마세요, 같은 작업입니다.**

### 가져옵니다 — 호흡 세션 (1.1, 코드가 이미 있습니다)

윤아님이 같은 날 보내신 두 번째 스크린샷입니다. **다른 곳에서 미니앱으로
이미 만드신 호흡 화면** — 4-7-8 / 박스 / 균형 토글, 위상 안내("숨을
들이마셔요"), 카운트다운, `CYCLE 1` 진행 바.

아래 오디오 항목의 반대 이유가 **여기엔 하나도 걸리지 않습니다.**
라이선스도, 호스팅도, 언어별 녹음도, 콘텐츠 제작비도 없습니다. 코드와
타이밍뿐이고 그건 이미 있습니다.

**넣어야 하는 이유는 따로 있습니다.** 지금 앱에서 사용자가 하는 일은
탭 → 입력 → 읽기가 전부입니다. 전부 넣거나 받는 동작이고 **눈 감는
순간이 없습니다.** "리추얼"이라 부르려면 하나는 있어야 합니다.

그리고 `A 1-minute morning ritual`이 비유가 아니라 화면의 숫자가 됩니다:

| 패턴 | 1사이클 | 1분에 |
| --- | --- | --- |
| 4-7-8 | 19초 | 3사이클 = 57초 |
| 박스 (4-4-4-4) | 16초 | 4사이클 = 64초 |
| **균형 (5-5)** | **10초** | **6사이클 = 정확히 60초** |

**포팅할 때 바꿔야 하는 세 가지:**

**① 기본값은 `균형`(5-5)입니다. 4-7-8이 아닙니다.** 4-7-8은 Andrew
Weil이 잠들기 위해 설계한 패턴입니다 — 날숨이 들숨의 두 배라 강하게
가라앉힙니다. 아침 기본값으로는 방향이 반대입니다. 토글에는 남겨둡니다:
pricing이 이미 *"Feeling anxious for no reason?"*라고 말하니 불안한
아침에 고를 이유가 있습니다. 기본이 아니라 선택지여야 한다는 것뿐입니다.
(균형이 정확히 60초로 떨어지기도 합니다.)

**② 무드 체크인 앞에 두지 않습니다.** 무드 체크인은 리텐션 전체가 걸린
단 하나의 행동입니다 — 스트릭, 캘린더, 월간 리뷰가 전부 거기서 나옵니다.
앱을 열자마자 60초 화면이 가로막으면 매일 하던 사람도 바쁜 날엔
건너뛰고, 하루 끊긴 스트릭은 잘 안 돌아옵니다.

**자리는 해석을 읽은 뒤, 어퍼메이션을 띄운 채입니다.** 그 자리가 지금
비어 있습니다 — 해석을 읽고 나면 끝이고, 어퍼메이션도 떴다가 사라집니다
(`AffirmationsDisplay`, `SimpleDreamInterface.tsx:4293`). 호흡 링이 둘을
붙잡아 주고, **건너뛰어도 아무것도 잃지 않습니다.**

**③ 색을 다시 칠합니다.** 미니앱은 검정 배경에 주황이고 Novakitz는
하늘색 대낮입니다. 메커니즘(위상 전환, 카운트다운, 사이클 바)만 가져오고
가운데 주황 글로우는 아트워크의 **해**로 바꿉니다 — 이미 그림 안에 있는
물건입니다.

**문구 주의.** "불안을 낮춰준다", "심박을 떨어뜨린다" 같은 효과 주장은
쓰지 않습니다. 호흡 앱 자체는 App Store에 수백 개 있어서 문제가 아니지만,
건강 효과를 단정하면 심사가 근거를 묻습니다. 하는 일만 적습니다.

**왜 1.0이 아닌가.** 코드가 있어도 새 화면이고, 실기기 확인이 필요하고,
스크린샷 다섯 장 중 한 자리를 요구합니다. 제출 블로커(아이콘)를 하나도
풀지 않습니다.

### 안 합니다 — 사운드스케이프와 가이드 명상

**호흡과 헷갈리면 안 됩니다.** 위 항목이 되는 이유는 오디오가 없기
때문입니다. 아래가 안 되는 이유는 전부 오디오에서 나옵니다.

세 가지입니다.

**코드가 아니라 콘텐츠 파이프라인입니다.** "Mountain Observatory" 같은
것은 라이선스 오디오이고, 가이드 명상은 대본 + 성우 + 언어별 녹음입니다.
TTS로 때우면 차분함을 파는 앱에서 가장 티 나는 자리가 됩니다. 그리고
한 번 만들고 끝이 아니라 계속 늘려야 하는 비용입니다.

**미디어 호스팅이 없습니다.** 배포는 API 라우트뿐입니다. 번들에 넣으면
다운로드 용량이 뛰고, 스트리밍하면 호스팅과 대역폭이 이 앱의 **첫
고정비**가 됩니다.

**저녁 앱의 화면입니다.** "End the day with warmth", "help you drift
off" — 잠드는 쪽입니다. 우리는 `Morning Ritual Kit`으로 정했고, 명상
라이브러리를 만드는 순간 Calm·Headspace와 같은 칸에 섭니다. 자금력으로
이기는 카테고리입니다.

**Kit에 오디오가 필요하지도 않습니다.** 지금 kit은 무드 체크인 + 꿈
기록/해석 + 어퍼메이션 + 월간 리뷰로 이미 네 개고, 넷 다 우리 데이터에서
나옵니다. 오디오는 누구나 살 수 있는 것입니다.

---

## 1. 꿈 목록이 `image` 컬럼을 통째로 들고 옵니다

**위치** `src/components/SimpleDreamInterface.tsx` — `loadDreams`

앱을 열 때마다 `select('*')`로 모든 꿈을 받아옵니다. 지금은 `.limit(200)` 상한만
걸어둔 상태입니다.

문제는 `image` 컬럼입니다. 스토리지 업로드가 실패하면 fallback으로
`canvas.toDataURL('image/jpeg', 0.7)` 결과를 **base64 문자열 그대로** 저장합니다
(`SimpleDreamInterface.tsx`의 이미지 업로드 경로). 한 장에 100~300KB이고, 목록을
불러올 때마다 전부 따라옵니다.

**왜 미뤘나** 목록에서 `image`를 빼려면 세 곳을 같이 고쳐야 합니다:

- `DreamBackgroundGallery`에 넘기는 배열 (장식용 배경)
- 수정 플로우 — 기존 이미지를 유지하려면 `dream.image`가 필요합니다
- 삭제 플로우 — 스토리지 정리에 `dream.image`가 필요합니다

잘못 건드리면 **수정할 때 이미지가 사라지거나 삭제 후 스토리지에 쓰레기가 남습니다.**
제출 6일 전에 감수할 위험이 아니었습니다.

**할 일**
1. 목록 쿼리에서 `image` 제외
2. 배경 갤러리용으로 `id, image`만 따로 조회 (최근 N개)
3. 수정·삭제 시점에 해당 꿈의 `image`를 단건 조회
4. 이왕이면 base64 fallback 자체를 없애기 — 업로드 실패 시 이미지 없이 저장하고
   나중에 재시도하는 편이 낫습니다

---

## 2. `SimpleDreamInterface.tsx`가 5,948줄입니다

전체 코드의 약 18%가 파일 하나에 있습니다. 2026-08-25 하루에만 이 파일에서 버그를
세 개 고쳤고, 그중 상당 시간이 파일 안에서 위치를 찾는 데 들었습니다.

**왜 미뤘나** 분리 자체가 위험한 게 아니라, 분리하면서 상태 전달이 끊기는 걸 못 잡을
위험이 큽니다. 테스트가 없습니다.

**할 일** 자연스러운 경계부터:
- 꿈 입력 모달
- 히스토리 목록 + 상세
- 수정 모달
- 홈 씬 래퍼

---

## 3. 확언 생성에 한도가 없습니다

**위치** `app/api/generate-affirmations/route.ts`

인증은 붙였지만(2026-08-25) **횟수 제한은 없습니다.** 해석은
`app/api/analyze-dream/route.ts`에서 `checkQuota`로 막히는데, 확언은 별개 라우트라
무료·유료 모두 무제한입니다.

같은 파일에 이런 코드가 있습니다:

```ts
const plan = await getUserPlan(userId);
affirmationCount = 3;   // 플랜을 조회한 뒤 무조건 덮어씀
```

즉 플랜 분기가 **죽은 코드**입니다. 무료도 3개를 받습니다.

**왜 미뤘나** 회당 Gemini 비용이 0.2센트 수준이라 지금은 금액 문제가 아닙니다. 그리고
사용자가 매일 실제로 보는 결과물이 확언 쪽이라, **출시 직전에 무료 경험을 줄이는 건
잘못된 방향**입니다.

**할 일** 실사용량을 2~4주 본 뒤 판단. 막는다면 해석과 같은 `checkQuota`를 쓰되,
별도 한도로.

---

## 4. `console` 호출 465개

**최다** `SimpleDreamInterface.tsx` 167개, `app/api/analyze-dream/route.ts` 33개

`console.log('User ID:', userId)`처럼 식별자를 그대로 찍는 것도 있었습니다(해당
라인은 2026-08-25에 제거). 실제 오류가 여기 묻힙니다 — CORS 오류를 찾는 데 시간이 더
걸린 이유이기도 합니다.

**할 일** 프로덕션 빌드에서 `console.log`만 제거 (`console.error`는 유지).
`next.config.ts`의 `compiler.removeConsole`로 한 줄입니다. 출시 전에 넣지 않은 건
디버깅 중이라서입니다.

---

## 5. 저장소 정리

| | |
| --- | --- |
| `src/components/MonthlyDreamReport.tsx.backup` | 커밋된 백업 파일 |
| `temp-force-deploy` | 배포 강제용으로 만든 빈 파일 |
| `next-pwa` | `package.json`에만 있고 코드·설정 어디서도 참조 안 함 |
| 루트 `.md` 20개 | `SECURITY*.md` 4종, `IMPLEMENTATION*.md` 4종 — 어느 게 최신인지 알 수 없음 |
| lint 경고 60여 개 | 대부분 안 쓰는 변수. `useEffect` 의존성 누락은 실제 버그가 될 수 있음 |

---

## 6. `arcanaArtChildren`이 이제 아무 데서도 안 쓰입니다

2026-08-25에 "카드 이미지 깨짐"으로 보고된 건이 **이미지 문제가 아니었습니다.**

`CardVisual`이 딱 한 곳에서, 항상 `blurred={true}`로만 호출되고 있었습니다. 즉
20개 아르카나 SVG(약 120줄)가 **한 번도 사람 눈에 보인 적이 없습니다** — 항상
블러 뒤에 있었습니다. 사용자가 본 건 자물쇠 이모지와 `UNCONSCIOUS`가 적힌
큰 회색 사각형뿐이었고요.

깨져 보인 원인은 블러 오버레이의 `backdrop-filter`입니다. 형제 SVG 위에 얹힌
`backdrop-filter`를 iOS가 불안정하게 합성해서, 어두운 카드는 회색으로 뜨면서
세로 띠가 생기고 밝은 카드는 우연히 멀쩡했습니다.

카드 자체를 없앴습니다(잠긴 문 뒤에 아무것도 없었으니). 그래서 지금
`arcanaArtChildren`은 **호출되지 않는 죽은 코드**입니다.

**할 일** 둘 중 하나:
- 진짜 리빌을 붙이기 — 기록을 마치면 카드가 실제로 열리게. 원래 의도였을 가능성이
  높고, 돌아올 이유가 하나 생깁니다
- 안 쓸 거면 `arcanaArtChildren`과 `arcanaCards`의 `from/mid/to` 삭제

아르카나 선택 자체는 살아 있습니다 — 확언과 저장되는 제목을 정합니다.

---

## 6-1. 비밀번호 재설정이 없습니다

2026-08-27에 이메일/비밀번호 로그인을 추가했습니다(`src/components/Auth.tsx`).
그런데 **비밀번호를 잊었을 때 되찾는 길이 없습니다.**

**왜 미뤘나** 재설정은 로그인 폼 하나로 끝나지 않습니다 —
`resetPasswordForEmail`을 보내고, 그 링크가 도착할 **새 비밀번호 입력 페이지**가
따로 있어야 하고, 그 페이지가 네이티브 앱에서도 열려야 합니다(딥링크). 제출 5일 전에
새 화면과 딥링크를 추가하는 건 좋은 거래가 아니었습니다.

출시 초기 규모에서는 문의가 오면 Supabase 대시보드에서 직접 처리 가능합니다.
소셜 로그인이 두 개나 있어서 대부분은 그쪽을 쓸 것이고요.

**할 일** `supabase.auth.resetPasswordForEmail` + `/auth/reset` 페이지 +
`capacitor.config.ts`에 딥링크. 사용자가 붙기 시작하면 바로.

---

## 6-2. 계정 삭제가 데이터만 지웁니다

`ProfileSettings.tsx`의 `handleDeleteAccount`는 `dreams`, `user_profiles`,
`nicknames`, `community_posts`, `community_likes`, `user_subscriptions`에서 행을
지우고 로그아웃시킵니다. **`auth.users`의 계정 자체는 남습니다.**

지금 못 고치는 이유는 권한입니다. 사용자를 실제로 지우려면
`supabase.auth.admin.deleteUser`가 필요하고 그건 service role 키를 씁니다 —
브라우저에서 부를 수 없습니다. 서버 라우트(`/api/account/delete`)를 새로 만들고
`apiAuth.ts`로 본인 확인을 한 뒤 지우는 게 맞는 구조인데, 제출 이틀 전에 새 라우트를
넣고 실기기 검증까지 하기에는 위험이 큽니다.

**심사에는 아마 걸리지 않습니다.** 가이드라인 5.1.1(v)가 요구하는 건 앱 안에서 계정
삭제를 *시작*할 수 있어야 한다는 것이고 그 버튼과 흐름은 있습니다. 다만 삭제를
요청한 사람의 계정이 남아 있는 건 그 자체로 맞지 않고, 그 이메일로 다시 로그인하면 빈
앱이 열립니다. `/support` 페이지의 문구는 실제 동작대로 — "꿈, 프로필, 커뮤니티 글,
구독 기록을 지웁니다" — 적어뒀습니다.

출시 직후 첫 작업으로 잡는 게 맞습니다.

---

## 7. 프로필 화면 디자인

`src/components/ProfileSettings.tsx`. 기능은 고쳤지만(스트릭 중복 계산 제거,
로그아웃 버튼 추가) 생김새는 그대로입니다.

---

## 7-0. 디자인 패스 — "웹기반이라 안 예쁘다"는 틀렸습니다

2026-09-01, 윤아님이 Veri(혈당 앱) 스크린샷을 보내면서 *"지금은 웹기반이라
너무 안 예뻐"*라고 하셨습니다. **진단이 틀렸고, 이게 중요합니다** — 기술이
원인이라고 결론내면 Swift 재작성을 고민하게 되는데 9/30 안에는 불가능합니다.

### 저 화면의 요소는 전부 CSS입니다

| 보이는 것 | 방법 |
| --- | --- |
| 크게 번지는 색 블룸 | `radial-gradient` — **홈 블룸이 이미 이 방식** |
| 벤토 그리드 카드 | CSS grid |
| 도트 매트릭스 숫자 | 폰트 하나 |
| 어두운 바탕 + 둥근 카드 | 기본 CSS |
| 카드 뒤 블러 | `backdrop-filter` (WKWebView 지원) |

증거가 앱 안에 있습니다. **pricing 페이지와 새 캘린더**는 같은 기술로
만들었고 저 둘은 안 못생겼습니다. 차이는 기술이 아니라 **디자인 패스를
했느냐**입니다.

### 웹 셸이라서 진짜로 어려운 것

**화면 전환 애니메이션** 하나입니다. 네이티브의 push/pop 전환과 공유 요소
전환은 웹에서 흉내내야 하고, 잘 안 하면 화면이 툭툭 바뀝니다. 스크롤 관성과
고무줄 효과는 WKWebView가 그냥 줍니다. 햅틱은 Capacitor 플러그인이 있고요.

나머지는 **안 한 것이지 못 하는 게 아닙니다.**

### 범위

§7-1(버튼 글로우)이 이 작업의 첫 조각입니다. 전체로는 홈·저널·리플렉션·
월간 리뷰의 카드와 여백, 타이포 스케일까지입니다. §0(탭 바)과 같은 버전에
묶는 것이 맞습니다 — 어차피 모든 화면을 건드리고, 스크린샷도 그때 다시
찍습니다.

### 방향은 정해졌습니다 (2026-09-01)

윤아님이 레퍼런스 두 장을 보내셨습니다. 크림 바탕에 번지는 색 덩어리와
모노스페이스 라벨을 얹은 "Contexts" 화면, 그리고 크림 들판에 물감 블롭
넷을 놓고 세리프로 질문을 던지는 감정 선택 화면.

**두 번째는 우리 무드 피커와 같은 뼈대입니다.** 크림 패널, 유기적 블롭,
부드러운 가장자리, 세리프 제목 — 제목 폰트는 같은 날 들어갔습니다
(`6e80f1c`, Instrument Serif). 남은 차이는 셋뿐이고 전부 CSS입니다.

**① 질감.** 레퍼런스의 블롭은 물감이고 우리 것은 평평한 `rgba()`입니다.
방법은 이미 저장소에 있습니다 — 홈 배경의 그레인이 `feTurbulence` +
`feComponentTransfer`로 만들어졌고, 그림 자체 하늘의 고주파 편차 10.9에
맞춰 **7.5**로 조정한 기록이 「기록해둘 결정들」에 있습니다. 같은 필터를
조약돌에 씌우면 됩니다.

**② 배경의 유령 블롭들.** 큰 블롭 뒤에 작고 흐린 블롭이 흩뿌려져 있어서
화면이 격자가 아니라 구성으로 보입니다. 우리는 4×2 격자가 그대로
드러납니다. 배경 레이어 하나면 됩니다.

**③ 크기와 개수.** 레퍼런스는 크게 넷, 우리는 작게 여덟입니다.

**③은 따라가지 마세요.** 레퍼런스의 넷은 High/Low Energy × Pleasant/
Unpleasant 축이고, 우리 여덟은 이름이 붙은 감정입니다. 이름이 더
구체적이고, **캘린더가 그 여덟 개의 모양에 의존합니다.** 가져올 것은
질감과 구성이지 분류 체계가 아닙니다.

**첫 번째 레퍼런스(번지는 색 덩어리)는 §7-1과 같은 기법**입니다 —
`radial-gradient`를 가상 요소에. 버튼과 이 화면이 같은 언어를 쓰게
됩니다.

---

## 7-1. 버튼에 글로우 — 전면 교체

2026-09-01, 윤아님이 레퍼런스를 보내주셨습니다. 크림 바탕 위 알약 모양
뒤에 파란 빛이 부드럽게 번지는 형태 — **버튼의 생김새이지 글자 스타일이
아닙니다.** 글자는 지금처럼 진한 색으로 두고 버튼에만 빛을 얹습니다.
(그래서 대비 문제는 생기지 않습니다. 흰 글자에 글로우를 주는 방식이었다면
크림 위 흰 글자가 1.22:1이라 못 쓸 뻔했습니다.)

### 만들 때 — 이미 한 번 데인 부분이 있습니다

**큰 `box-shadow` 블러를 쓰지 마세요.** 새벽 배경 작업 때(§「기록해둘
결정들」) 같은 종류의 빛을 box-shadow로 그렸다가, 브라우저가 타일 단위로
합성하면서 **희미한 사각형 이음매**가 하늘에 보였습니다. 해결책은
`radial-gradient`를 가상 요소에 그리는 것이었고, 같은 방법이 버튼에도
그대로 적용됩니다.

### 왜 출시 전에 안 했나

**모든 화면에 있습니다.** 버튼은 홈, 결제, 프로필, 지원, 인증, 무드
플로우 어디에나 있어서 한 번에 다 바뀌고, 미묘하게 깨진 것은 실기기에서만
보입니다.

**빌드가 이미 올라가 있습니다.** 지금 고치면 재빌드 → 재아카이브 →
재업로드 한 바퀴이고, 그만큼 제출이 밀립니다. **일찍 제출하는 것이 리젝
라운드에 대한 유일한 보험**이라 그 하루가 비쌉니다.

**스크린샷은 어차피 1.1에서 다시 찍습니다.** §0(탭 바)이 내비게이션을
바꾸면 다섯 장이 전부 무효가 됩니다. 그러니 지금 찍는 스크린샷에 옛
버튼이 담기는 것은 손해가 아닙니다 — 어차피 교체될 것들입니다. **§0과
같은 버전에 묶으면 추가 비용이 0입니다.**

---

## 8. Sign in with Apple — 웹

네이티브는 2026-08-25에 실기기에서 동작 확인했습니다. **웹은 설정이 안 되어 있습니다.**

필요한 것:
- Apple 개발자 사이트에서 Service ID 생성
- `.p8` 키 발급
- Supabase → Authentication → Providers → Apple

코드는 이미 있습니다 (`src/lib/appleAuth.ts`).

---

## 9. shadow work

`SHIPATON.md` §9에 설계가 있습니다. 심사 기간이 10/1~10/13이라 그 사이 업데이트가
나가면 "살아 움직이는 앱"으로 보입니다.

---

## 9-1. 리텐션 — 다시 돌아오게 만드는 것

출시 후 첫 번째로 붙어야 할 작업입니다. 순서까지 정해뒀습니다.

### 먼저, 데이터가 말하는 것

외부 사용자 14명이 3일 이상 썼습니다. 평균 13일, 최대 42일. 그리고 **전원
이탈했습니다** (마지막 활동 2026-07-22).

**그 14명이 누구였는지가 중요합니다** (2026-08-30 확인). 지인은 **1명뿐**이고
나머지는 전부 **SNS를 보고 스스로 찾아온 모르는 사람들**입니다. 국적도 한국과
외국이 섞여 있습니다.

세 가지가 달라집니다:

- **지인은 의리로 며칠 씁니다. 모르는 사람은 안 그럽니다.** 아무 관계도 없는
  사람이 평균 13일, 최대 42일을 썼다면 그건 다른 종류의 증거입니다
- **영어권에 통할지는 이미 일부 증명돼 있습니다.** 그 안에 외국 사용자가
  있었습니다. "한국 지인 데이터라 해외는 미지수"라고 읽으면 틀립니다
- **포지셔닝이 이미 한 번은 작동했습니다.** SNS를 보고 낯선 사람이 앱까지
  왔다는 뜻이고, 부제·설명·포지셔닝이 백지에서 시작하는 게 아닙니다

중요한 건 이들이 **알림도 없고, 홈 화면 아이콘도 없이** 그만큼 썼다는 점입니다.
42일을 쓴 사람은 매일 브라우저를 열고 주소를 치거나 북마크를 찾아 들어왔다는
뜻입니다.

그러니까 **돌아올 *이유*가 없어서 나간 게 아니라, 돌아올 *신호*가 없어서
나간 것**으로 읽는 게 맞습니다. 앱을 열 계기가 아예 존재하지 않았습니다.
아이디어가 거부당한 게 아니라 리텐션 장치가 없었던 것에 가깝습니다.

### 우선순위

> 위 **0번(하단 탭 바)** 이 이 목록보다 먼저입니다. 알림이 사람을 데려와도
> 돌아온 자리에서 볼 것이 서랍 뒤에 있으면 절반은 새어 나갑니다.

**1위 — 아침 알림.** 모닝 리추얼 앱인데 리마인더가 없습니다.
`src/lib/notifications.ts`와 `@capacitor/local-notifications`는 이미 있고,
`ReminderSettings` 컴포넌트도 있는데 실제로 매일 발송되게 걸려 있지 않습니다.
가장 큰 한 방이고, 코드도 대부분 있습니다.

> 출시 전에 손대면 안 됩니다. 알림 권한 요청 플로우는 심사에서 보는 부분이라,
> 첫 제출에 새 권한 요청을 끼워넣는 건 리스크입니다.

**2위 — 카드 리빌 완성.** 위 6번 항목과 같은 이야기입니다. 설계는 원래
"오늘의 카드가 잠겨 있다 → 기록하면 열린다"였는데, **여는 쪽 코드가 붙지
않아서** 영원히 안 열리는 자물쇠만 남아 있었습니다(2026-08-25에 제거).

완성하기 좋은 이유:

- **이미 90% 있습니다.** 20개 카드 아트, 카드별 확언, 감정→아르카나 매핑까지.
  리빌 단계 하나만 붙이면 됩니다
- **매일 달라집니다.** `selectArcanaId`가 날짜 + 유저 시드로 뽑아서, 같은 감정을
  골라도 날마다 다른 카드가 나옵니다. "오늘은 뭐가 나올까" 가 돌아올 이유입니다
- **앱의 톤에 맞습니다.** 스트릭은 놓치면 벌을 주고, 카드는 오면 선물을 줍니다.
  Duolingo식 압박에 대한 거부감 문제를 피해갑니다
- **크기가 심사 기간에 딱 맞습니다.** 10/1~10/13 사이 업데이트로 내면
  "살아 움직이는 앱"으로 보입니다

**3위 — 홈 화면 아이콘.** 네이티브 앱으로 출시되면 자동으로 해결됩니다.
churn한 14명에게 없었던 것 중 하나가 이거였습니다.

### 스트릭에 대한 메모

스트릭(`src/lib/streak.ts`)은 이미 있고, 한 달에 하루는 봐주게 되어 있습니다.
하지만 스트릭 단독으로는 리텐션이 안 됩니다 — **놓친 사람에게 알려줄 방법이
없으면 존재하지 않는 것과 같습니다.** 알림(1위)이 붙어야 스트릭이 작동합니다.
순서가 그래서 중요합니다.

---

## 9-2. 커뮤니티(살구정원)를 살릴지 지울지

`app/community/page.tsx`와 `src/components/CommunityFeed.tsx`가 있고, 빌드에
12.4 kB로 들어갑니다. **어디에서도 링크되지 않습니다** — 앱 메뉴에도, 웹사이트
어느 페이지에도, 사이트맵에도 없습니다. URL을 직접 쳐야만 열리는 고아 라우트이고,
윤아님도 지금 안 쓰신다고 하셨습니다.

**출시 전에는 건드리지 않습니다.** 제출 이틀 전에 라우트를 지우는 건 얻는 것 없이
빌드를 흔드는 일입니다. 12 kB 말고는 해가 없습니다.

**앱 메뉴에 붙이는 순간 가이드라인 1.2 대상이 됩니다.** 사용자 생성 콘텐츠가 앱 안에
있으면 Apple이 요구합니다 — 콘텐츠 필터링, 신고 기능, 사용자 차단, 공개된 연락처.
`CommunityFeed.tsx`에 셋 다 없습니다(`report`/`block` 문자열 자체가 없습니다).
지금 1.2에 안 걸리는 유일한 이유가 앱에서 도달이 안 되기 때문입니다.

출시 후 선택지는 둘입니다: 신고·차단을 만들고 메뉴에 붙이거나, 라우트와
`community_posts` / `community_likes` / `community-images` 버킷을 정리하거나.
중간은 없습니다 — 지금처럼 두면 아무도 안 쓰는 코드가 계속 빌드에 실립니다.

---

## 10. 월 달력 — 셀 안의 사진

달력 시트 자체는 8/28에 다시 만들었습니다 (크림 종이, 굵은 월 타이포, 실선 그리드,
감정 색으로 채운 날, 그 달이 실제로 쓴 색만 나오는 범례).

**셀에 사진을 넣는 것은 안 했습니다.** 참고한 인스타 달력의 핵심이 그건데, 달력
쿼리가 `image` 컬럼을 일부러 안 가져옵니다 — 업로드가 실패하면 base64 데이터 URL이
통째로 들어가는 컬럼이라, 그리드 하나 그리려고 서른 개를 받으면 실행할 때마다 수 MB가
나갑니다. 사진 달력을 하려면 먼저 썸네일을 스토리지에 따로 저장하는 경로가 있어야
합니다. 그건 출시 후입니다.

`database/add_emotion_to_checkins.sql`이 조약돌 색을 기억하기 위한 마이그레이션입니다.
**이 파일을 Supabase에서 실행해야 합니다** — 지금 달력은 `dreams.mood`로 색을 칠하기
때문에 꿈을 안 적고 조약돌만 누른 날은 비어 보입니다. `checkins.mood`는 1~5 척도라
anxious/lonely/anger가 전부 2로 겹쳐서 그대로는 색을 복원할 수 없습니다.

---

## 기록해둘 결정들

**부제는 `Morning Ritual Kit`입니다 (2026-08-31).** 이름의 `kitz`와 붙고,
`journal`이 한 가지 물건인 데 반해 `kit`은 세트라고 말합니다 — 무드 체크인,
꿈 기록/해석, 어퍼메이션, 월간 리뷰로 실제 네 개라 과장이 아닙니다. 윤아님이
걱정하신 "꿈일기는 니치해서 다운로드가 안 될 것 같다"를 겨냥한 선택입니다.

대가가 있었습니다. **부제가 `mood`와 `dream`의 유일한 색인 진입로였습니다**
(앱 이름에도 키워드에도 없었습니다). 그래서 둘을 키워드로 옮기고 자리를 만들려
`jungian`을 뺐습니다 — 셋 중 검색량이 가장 낮아서입니다.

```
mood,dream,tracker,journal,reflection,meaning,affirmation,self care,habit,calm,sleep,anxiety,symbol
```

99자. **이게 오히려 개선입니다** — App Store는 키워드 단어를 조합해 매칭하는데,
`dream`이 부제 안에 갇혀 있을 때는 `meaning`이나 `symbol`과 만날 수 없었습니다.
옮기고 나면 `dream meaning` / `dream journal` / `dream symbol` / `mood tracker`가
전부 생깁니다.

**그리고 이 이름이 §0(하단 탭 바)을 더 급하게 만듭니다.** "Kit"은 여러 개가
들어 있다는 약속인데, 앱을 열면 원 하나가 보이고 나머지 셋은 서랍 뒤에 있습니다.
기능이 실제로 다 있으니 가이드라인 문제는 아니지만, 다운로드를 늘려놓고 첫 화면이
약속을 안 지키면 그 자리에서 이탈합니다.

**태그라인은 `A 1-minute morning ritual for a better day.`입니다.**
`A 1-minute morning ritual`만으로는 무엇만 말하고 왜를 안 말합니다.
(원안 "for better day"는 관사가 빠져 영어가 되지 않았습니다. 영어권 타겟인데
스토어 첫 줄에 문법 오류가 있으면 앱 완성도까지 의심받습니다.)

**무료 티어는 월 7회 해석입니다.** `database/update_free_plan_to_7.sql`.
`checkQuota`의 기본값도 7입니다.

**"unlimited"라는 단어는 어디에도 쓰지 않습니다.** Pro는 월 200회 상한이 있어서
사실이 아니고, 가이드라인 2.3.1에 걸립니다. "daily"로 표현합니다.

**`novakitz.premium.monthly`는 영구히 못 씁니다.** Family Sharing을 켰다가 되돌리려
삭제했는데, App Store Connect는 삭제한 상품의 ID와 Reference Name을 계속 예약해
둡니다. 그래서 월간만 `pro`, 연간은 `premium`입니다. 사용자에게 안 보이는 값이라
맞추지 않았습니다.

**인증은 fail closed, 한도는 fail open입니다.** `src/lib/apiAuth.ts`는 Supabase
자격증명이 없으면 거부하고, `src/lib/subscriptionServer.ts`는 통과시킵니다. 배포
문제로 아침 리딩을 못 받게 하는 것보다는 낫지만, 아무도 검증 못 하는 배포가 "그럼 다
통과"로 결론내면 안 되기 때문입니다.

**홈 아트워크는 rosasawyers Instagram을 참고했다가 다시 그렸습니다.** 이전 버전은
주황 지붕 집 + 타원형 연못 + 같은 그레인 + 같은 채도로 참고작과 지나치게
가까웠습니다.

**새벽 배경은 되돌렸습니다 (8/28).** 하늘색 → 라벤더 → 분홍 그라데이션에 초록 언덕을
얹은 버전이었는데, 이미 복잡한 그림 뒤에 색 면 세 개와 곡선 하나를 더 놓는 셈이라
화면에 주인공이 없어졌습니다. 단색 하늘색으로 복귀. 그 버전에서 두 가지는 남겼습니다 —
**블룸을 radial-gradient로 그리는 것**(큰 box-shadow 블러는 타일 단위로 합성돼서
하늘에 희미한 사각형 이음매가 보였습니다)과 **그레인을 눈이 아니라 측정으로 맞추는
것**입니다. 그림 자체 하늘의 고주파 편차가 10.9인데 plain fractalNoise + soft-light는
1.8이 나와서 배경만 인쇄물이 아닌 것처럼 보였습니다. `feComponentTransfer`로 대비를
늘리고 서로 다른 주파수의 레이어 두 장을 겹쳐 **7.5** — 확실히 보이되 그림보다는
조용하게. 아트워크 자체 교체는 여전히 열려 있습니다.

그레인 레이어는 **디스크 아래**에 깝니다. 처음엔 씬 전체를 덮게 뒀는데, 그림이 이미
자기 그레인을 갖고 있어서 두 겹이 되면서 원 안이 탁해졌습니다. `z-index`로 포털 밑에
넣으면 평평한 하늘과 블룸에만 닿습니다.

**새벽 배경의 나머지 코드는 지웠습니다.** `.home-scene__ground`와 `--horizon`,
새벽 팔레트 변수들. 되살릴 일이 생기면 `c0137e6`에 그대로 있습니다.
